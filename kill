docker rm -f $(docker ps -a -q)
[ -n "$1" ] && docker images -q | xargs docker rmi
[ -n "$1" ] && docker images -q | xargs docker rmi
[ -n "$1" ] && docker images -q | xargs docker rmi
[ -n "$1" ] && docker images -q | xargs docker rmi
